steps:
- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  args:
  - gcloud
  - functions
  - deploy
  - merchantmap
  - --region=us-central1
  - --source=website
  - --trigger-http
  - --runtime=nodejs14
  - --no-allow-unauthenticated 
  - --entry-point app
